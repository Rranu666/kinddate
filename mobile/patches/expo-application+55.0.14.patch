diff --git a/node_modules/expo-application/android/build.gradle b/node_modules/expo-application/android/build.gradle
index 4efd7af..af3efc6 100644
--- a/node_modules/expo-application/android/build.gradle
+++ b/node_modules/expo-application/android/build.gradle
@@ -1,8 +1,16 @@
 plugins {
   id 'com.android.library'
-  id 'expo-module-gradle-plugin'
 }
 
+// expo-module-gradle-plugin is not registered in bare RN builds (the Gradle
+// plugin project is missing from the expo-modules-core npm package).
+// Fall back to the classic ExpoModulesCorePlugin.gradle approach.
+def expoModulesCorePlugin = new File(project(":expo-modules-core").projectDir.absolutePath, "ExpoModulesCorePlugin.gradle")
+apply from: expoModulesCorePlugin
+applyKotlinExpoModulesCorePlugin()
+useDefaultAndroidSdkVersions()
+useCoreDependencies()
+
 group = 'host.exp.exponent'
 version = '55.0.14'
 
diff --git a/node_modules/expo-application/android/src/main/java/expo/modules/application/ApplicationModule.kt b/node_modules/expo-application/android/src/main/java/expo/modules/application/ApplicationModule.kt
index b151a60..9751284 100644
--- a/node_modules/expo-application/android/src/main/java/expo/modules/application/ApplicationModule.kt
+++ b/node_modules/expo-application/android/src/main/java/expo/modules/application/ApplicationModule.kt
@@ -24,24 +24,14 @@ class ApplicationModule : Module() {
   override fun definition() = ModuleDefinition {
     Name("ExpoApplication")
 
-    Constant("applicationName") {
-      context.applicationInfo.loadLabel(context.packageManager).toString()
-    }
-
-    Constant("applicationId") {
-      packageName
-    }
-
-    Constant("nativeApplicationVersion") {
-      packageManager.getPackageInfoCompat(packageName, 0).versionName
-    }
-
-    Constant("nativeBuildVersion") {
-      getLongVersionCode(packageManager.getPackageInfoCompat(packageName, 0)).toInt().toString()
-    }
-
-    Constant("androidId") {
-      Settings.Secure.getString(context.contentResolver, Settings.Secure.ANDROID_ID)
+    Constants {
+      mapOf(
+        "applicationName" to context.applicationInfo.loadLabel(context.packageManager).toString(),
+        "applicationId" to packageName,
+        "nativeApplicationVersion" to packageManager.getPackageInfoCompat(packageName, 0).versionName,
+        "nativeBuildVersion" to getLongVersionCode(packageManager.getPackageInfoCompat(packageName, 0)).toInt().toString(),
+        "androidId" to Settings.Secure.getString(context.contentResolver, Settings.Secure.ANDROID_ID)
+      )
     }
 
     AsyncFunction<Double>("getInstallationTimeAsync") {
